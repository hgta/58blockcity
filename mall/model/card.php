<?php
/**
 * 模特卡片渲染已随模特库迁移至独立子站 model.58.tl
 * 保留空函数定义，避免历史 include 产生致命错误（新代码请勿依赖）
 */
if (!function_exists('renderModelCard')) {
    function renderModelCard($m, $imgStrip = [], $isFollowed = false, $userId = 0)
    {
        return '';
    }
}
