## 1. 数据库与状态模型

- [ ] 1.1 扩展 `bct_orders.status` 枚举，加入 `expired`（enum 依次为 pending/processing/completed/canceled/expired），验证插入该状态值成功
- [ ] 1.2 确认 `expires_at` 字段与 `idx_expires` 索引已存在且可用，无需新增字段；验证按 `expires_at` 过滤的查询可走索引
- [ ] 1.3 统一过期判定的时间口径：所有过期判定改为由 PHP 传入 date 字符串与 `expires_at` 比较，不在 SQL 中使用 NOW()；验证写入与判定在同一时区下结果一致

## 2. 有效期定义与写入

- [ ] 2.1 在 `classes/BCTOrder.php` 中定义有效期选项到过期时间的映射（1天/2天/7天 按天数、3个月 用 strtotime 的 +3 months 按自然月、长期 写空值），并暴露给前端使用；验证各选项计算出的过期时间符合预期
- [ ] 2.2 改造 `createOrder()` 使其接收有效期标识并写入 `expires_at`，保持 长期 写入空值的行为；验证 长期 订单 expires_at 为空、有限期订单等于发布时刻加所选时长
- [ ] 2.3 确认既有调用方（单条发布、批量发布）传入有效期后不破坏原有逻辑；验证不传有效期时的默认行为明确可控

## 3. 过期处理能力

- [ ] 3.1 在 `classes/BCTOrder.php` 新增过期处理能力，仅更新 status 属于 pending/processing 且 expires_at 非空且已早于当前时间的订单，置为 expired；验证 completed 与 canceled 订单不受影响
- [ ] 3.2 为过期处理能力增加作用域参数（全站 / 指定用户 / 指定城市），使惰性推进可按需限定范围；验证限定用户时只处理该用户的订单
- [ ] 3.3 验证过期处理幂等：对同一批数据连续执行两次，第二次不产生额外变更，且状态与首次结果一致
- [ ] 3.4 验证长期订单（expires_at 为空）与未到期订单均不被处理

## 4. 过期推进途径

- [ ] 4.1 新增惰性推进：`bct/user/orders.php` 在查询列表前推进当前用户已过期订单，验证用户访问后列表中不再出现已超期的待成交订单
- [ ] 4.2 新增定时任务 `bct/cron/expire_orders.php`，执行全站过期处理并输出处理条数与日志；验证可手动执行并正确统计
- [ ] 4.3 为定时任务加入 flock 防重入保护（参照 `bct/cron/auto_match.php`），验证上一次未结束时本次直接跳过并输出提示
- [ ] 4.4 验证惰性与定时任务同时触发同一批订单时结果一致，不产生重复取消或状态错乱

## 5. 发布界面有效期选择

- [ ] 5.1 在 `bct/trade.php` 单条模式新增有效期选择控件（1天/2天/7天/30天/3个月/长期），默认选中 30天，并附「到期后订单将自动取消」的文字说明；验证默认值与选项完整
- [ ] 5.2 单条模式提交时把有效期随表单传入 `process_order.php` 并写入订单；验证发布的订单过期时间与所选时长一致
- [ ] 5.3 在 `bct/trade.php` 批量模式新增有效期选择控件（整批共用，默认 30天）；验证批量提交后该批次所有订单过期时间口径一致
- [ ] 5.4 批量提交处理中把有效期应用到每一行；验证批量订单的过期时间与整批所选时长一致

## 6. 过期状态展示与筛选

- [ ] 6.1 `bct/user/orders.php` 的状态映射新增「已过期」，并在筛选列表中提供该选项；验证用户可筛选出因超期取消的订单
- [ ] 6.2 `bct/admin/orders.php` 的状态筛选与徽章样式新增「已过期」，统计口径中过期与取消分别计数；验证后台可筛选并按状态区分计数
- [ ] 6.3 验证过期订单在「已过期」与「已取消」两个筛选下互不混淆

## 7. 端到端验证

- [ ] 7.1 单条模式发布 1天 有效期订单，验证过期时间约为发布后 1 天且订单初始为待成交
- [ ] 7.2 构造一笔已超期的 pending 订单，验证访问订单列表后被置为「已过期」
- [ ] 7.3 构造一笔已超期的 processing（部分成交）订单，验证过期后剩余数量作废且状态为「已过期」
- [ ] 7.4 构造一笔已超期的 completed 订单，验证状态保持「已完成」不被改为过期
- [ ] 7.5 发布一笔「长期」订单并验证其不设置过期时间、不会被任何过期途径处理
- [ ] 7.6 验证 3个月 选项按自然月计算（跨不同长度月份时与固定 90 天结果不同）
- [ ] 7.7 回归验证既有功能未受影响：批量发布、单条发布、订单列表与后台交易管理页正常
