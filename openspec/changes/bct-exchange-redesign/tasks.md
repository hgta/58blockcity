## 1. 后端数据层扩展

- [x] 1.1 在 `classes/CityBCT.php` 中新增 `getMarketStats()`，返回 24h 成交额、涨跌城市数、总市值、活跃订单数，并验证 SQL 返回预期字段。
- [x] 1.2 在 `classes/CityBCT.php` 中新增 `getTopGainersLosers($limit)`，按 24h 涨跌幅返回涨跌榜，验证前 5 条包含北京/上海/广州/深圳/杭州时排序正确。
- [x] 1.3 在 `classes/CityBCT.php` 中新增 `getPriceHistory($city, $interval)`，按 1h/24h/7d/30d 聚合 `bct_transactions` 为 OHLC 或折线数据，验证无成交城市返回空数组。
- [x] 1.4 在 `classes/BCTOrder.php` 中新增 `getOrderBook($city, $type, $limit)`，按价格聚合 pending/processing 订单并返回档位数组，验证同价订单数量正确累加。
- [x] 1.5 在 `classes/BCTOrder.php` 中新增 `getRecentTrades($city, $limit)`，返回最新成交记录（含方向/数量/价格/时间），验证城市为空时返回全市场最新成交。
- [x] 1.6 在 `classes/UserBCTAccount.php` 中新增 `getPortfolioSummary($userId)`，返回总资产估值、可用余额、冻结余额、持仓城市数，验证与持仓列表计算一致。

## 2. 交易所主题资源

- [x] 2.1 创建 `/bct/assets/css/exchange-theme.css`，定义深色主题 CSS 变量、表格/卡片/按钮/表单覆盖样式，验证在 BCT 页面引入后内容区变为深色。
- [x] 2.2 创建 `/bct/assets/js/exchange-charts.js`，封装 ECharts 初始化函数（折线图、K 线图、饼图），验证通过 CDN 引入后可渲染示例图表。
- [x] 2.3 更新 `/bct/includes/header.php`，为 BCT 子站引入深色主题样式与 ECharts CDN，验证页面源码包含新增资源链接。

## 3. 首页市场概览（index.php）

- [x] 3.1 改造 `/bct/index.php` 顶部为滚动报价带，展示至少 20 个城市的价格与涨跌幅，验证横向滚动正常且颜色符合涨跌色规范。
- [x] 3.2 在首页添加全局市场统计面板（24h 成交额、涨跌城市数、活跃订单数、总市值），验证数字与 `getMarketStats()` 返回一致。
- [x] 3.3 在首页添加 TOP5 城市卡片（北京/上海/广州/深圳/杭州），每张卡片展示价格、涨跌幅、成交量、市值，验证点击跳转至 `city.php?city=xxx`。
- [x] 3.4 在首页添加最新成交动态区，展示最近 10 条全市场成交，验证买入/卖出方向颜色正确。
- [x] 3.5 确保首页移动端适配：报价带可滑动、卡片 2 列、统计面板不溢出，验证 iPhone SE 宽度下无横向滚动。

## 4. 城市代币列表（market.php）

- [x] 4.1 改造 `/bct/market.php` 为交易所风格列表页，展示排名、城市、价格、24h 涨跌、成交量、市值、流通量，验证默认按市值降序。
- [x] 4.2 实现列表分页（每页 50 条）与 URL 参数保留，验证总页数与切换行为正确。
- [x] 4.3 实现表头排序（价格、涨跌幅、成交量、市值升/降序），验证点击表头后 URL 与列表同步更新。
- [x] 4.4 实现城市搜索框，支持中文与拼音首字母过滤，验证输入“beij”与“北京”均显示北京。
- [x] 4.5 TOP5 城市在列表顶部以高亮行/卡片固定展示，验证默认排序时前 5 行样式与普通行不同。

## 5. 单城市交易详情（city.php）

- [x] 5.1 新增 `/bct/city.php`，顶部展示城市币基本信息（城市名、当前价、24h 涨跌、最高/最低、成交量、市值、供应量），验证访问 `?city=北京` 数据正确。
- [x] 5.2 在城市详情页集成价格走势图，支持 1h/24h/7d/30d 切换，验证各时间维度图表正常渲染。
- [x] 5.3 在城市详情页添加快速交易区（买入/卖出 Tab、数量、单价、预览），验证提交后创建订单并跳转/刷新。
- [x] 5.4 设置页面标题与 meta 描述包含城市名与当前价格，验证浏览器标签与 HTML source 正确。
- [x] 5.5 确保城市详情页移动端垂直堆叠布局正常，验证 375px 宽度下图表、买卖盘、交易区依次展示。

## 6. 买卖盘深度与最新成交

- [x] 6.1 在城市详情页展示买卖盘深度（默认各 10 档），卖盘红色、买盘绿色，验证价格档位与累计数量正确。
- [x] 6.2 实现“展开更多”按钮显示完整买卖盘，验证点击后展示全部档位。
- [x] 6.3 在城市详情页展示该城市最新成交列表（最近 20 条），验证买入/卖出方向颜色与时间格式化正确。
- [x] 6.4 实现最新成交 30 秒轮询刷新（可选），验证新成交出现在列表顶部且不整页刷新。

## 7. 投资组合后台（user/dashboard.php）

- [x] 7.1 改造 `/bct/user/dashboard.php` 顶部为资产总览卡片（总资产估值、可用、冻结、持仓城市数），验证数字计算正确。
- [x] 7.2 添加持仓列表，展示城市、余额、当前价、估值、持仓占比，验证占比总和为 100%。
- [x] 7.3 添加持仓占比饼图，验证 ECharts 饼图正确渲染且图例可交互。
- [x] 7.4 保留并优化订单列表 Tab（进行中/已完成），验证取消订单与分页功能正常。
- [x] 7.5 确保投资组合页移动端适配，验证资产卡片与图表在窄屏下正常堆叠。

## 8. 交易发布页集成

- [x] 8.1 改造 `/bct/trade.php` 样式以匹配深色交易所主题，验证表单与选择器样式统一。
- [x] 8.2 支持从城市详情页携带 `city` 参数预填城市，验证跳转后自动选中对应城市。
- [x] 8.3 在交易发布页右侧增加当前城市行情小卡片，验证选择城市后显示价格与 24h 涨跌。

## 9. 集成验证

- [x] 9.1 在本地或测试环境访问 BCT 首页、行情列表、城市详情、个人中心、交易页，验证页面无 500 错误且深色主题生效。
- [x] 9.2 验证 TOP5 城市在首页与列表页均突出展示，且链接正确。
- [x] 9.3 验证分页、排序、搜索、图表、买卖盘、最新成交、投资组合等核心功能在桌面端与移动端均可用。
- [x] 9.4 运行 `openspec validate --change bct-exchange-redesign`，确认所有 artifacts 通过校验。
