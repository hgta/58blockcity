## 1. 价格历史表与迁移

- [x] 1.1 新增 `bct_price_history` 表（字段：id、city、price、base_price、created_at），并建立支撑「按城市取最新」与「按城市+时间范围取」的索引；验证建表语句可执行且索引存在
- [x] 1.2 编写幂等迁移脚本 `init/migration-bct-price-history.sql`，沿用现有迁移的 information_schema 检查风格，可重复执行不报错
- [x] 1.3 迁移脚本为存量城市各补一条以当前 `bct_current_price` 为准的初始快照；验证城市数与快照条数一致，且重复执行不产生重复快照
- [x] 1.4 在 `init/db-init.sql` 中同步新增表定义，使全新安装环境同样具备该表

## 2. 价格变更埋点

- [x] 2.1 在 `CityBCT::updatePrice()` 内写入一条价格历史记录（含当时的基础价）；验证后台单行保存价格后历史表新增记录
- [x] 2.2 验证后台批量设置价格同样产生历史记录（无需在 `bct/admin/city_prices.php` 单独埋点）
- [x] 2.3 验证 `autoAdjustPrice()` 自动调价产生历史记录
- [x] 2.4 历史写入失败时不影响价格更新主流程；验证构造写入异常时价格仍被成功更新
- [x] 2.5 价格未实际变化时避免写入重复记录（若自动调价在价格不变时仍调用 `updatePrice()`）；验证连续两次同价调用只产生一条记录

## 3. 涨跌幅计算改造

- [x] 3.1 改造 `get24hChanges()` 改用价格历史表，实现时间点对齐：当前价取最新一条历史，前价取 24 小时前及之前的最后一条；验证返回 `city => change_pct` 映射
- [x] 3.2 实现当前价回退：最近 24 小时内无记录时，当前价回退为最新一条历史价；验证 24h 未调价的城市仍出现在结果中
- [x] 3.3 前价取自更早记录：当 24~48h 区间无记录但更早存在记录时仍能算出涨跌幅；验证不因窗口缺记录而丢弃城市
- [x] 3.4 无任何历史记录的城市不参与统计；验证其不出现在返回结果中
- [x] 3.5 确保 `get24hChanges()` 保持「一次批量返回全部城市」的契约，不对每个城市单独查询；验证城市数从 1 增至 400+ 时查询次数不随之增长
- [x] 3.6 验证涨跌幅计算在价格持平时返回 0（而非 NULL 或异常值）

## 4. 依赖涨跌幅的指标

- [x] 4.1 验证 `getMarketStats()` 的涨跌城市统计正确：涨跌幅 >0 计入上涨、<0 计入下跌、=0 不计入；验证首页 ▲/▼ 数量与手工统计一致
- [x] 4.2 验证 `getTopGainersLosers()` 随 `get24hChanges()` 同步生效，涨幅榜按降序、跌幅榜按升序，且与涨跌城市统计同源
- [x] 4.3 验证 `bct/index.php`、`bct/market.php`、`bct/city.php`、`bct/trade.php` 四处消费方的 `change_pct` 取值正常，且无记录城市仍由 `?? 0` 兜底不报错

## 5. 高低价与走势图改造

- [x] 5.1 改造 `getCity24hHighLow()` 改用价格历史表，返回最近 24 小时的最高价与最低价；验证无 24h 内记录时返回 0
- [x] 5.2 改造 `getPriceHistory()` 改用价格历史表，支持 1h / 24h / 7d / 30d 区间的时间聚合；验证各区间均返回正确的时间序列
- [x] 5.3 `getPriceHistory()` 的 volume 字段显式返回 0 并注明原因（成交量依赖成交流水，当前无成交记录）；验证不以调价次数等其它数据伪造填充
- [x] 5.4 验证无数据区间返回空序列而非伪造数据点
- [x] 5.5 验证 `bct/city.php` 的城市详情页高低价与走势图正常渲染，无 JavaScript 报错

## 6. 端到端验证

- [x] 6.1 执行迁移后访问首页，验证涨跌城市数量不再恒为 0（有历史数据的城市参与统计）
- [x] 6.2 在后台修改数个城市的价格（含调高与调低），验证其随后出现在涨跌统计与涨跌榜中，且方向正确
- [x] 6.3 验证行情页与城市详情页的涨跌幅、24h 高低价与走势图均有数据显示
- [x] 6.4 回归验证：总市值与城市列表价格未受影响；批量发布、单条发布、订单列表、后台交易管理均正常
- [x] 6.5 确认本次未改动的指标保持原状且不报错：24h 成交量仍为 0、最近成交列表仍为空（属后续变更范围）
