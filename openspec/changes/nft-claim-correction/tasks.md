## 1. 数据层

- [x] 1.1 新增 `nft_claim_corrections` 建表 SQL（`id`/`claim_id`/`nft_id`/`city_id`/`from_user_id`/`to_user_id`/`admin_id`/`reason`/`created_at`，`CREATE TABLE IF NOT EXISTS`），验证可重复执行不报错
- [x] 1.2 在 `classes/NFT.php` 新增认领记录检索方法，支持 NFT编号/城市/用户名/用户ID/区块ID/时间区间组合筛选与分页，验证返回字段含 NFT 名称与归属用户名
- [x] 1.3 在 `classes/NFT.php` 新增可疑认领筛查方法，按「认领时间 − 注册时间 ≤ 阈值」返回结果并附带时间差，验证阈值参数生效
- [x] 1.4 在 `classes/NFT.php` 新增单条订正方法（事务内先按 (nft_id, city_id) 置 `is_current=0` 再插入新记录并写审计），验证订正后仅一条 `is_current=1`
- [x] 1.5 在 `classes/NFT.php` 新增批量订正与订正历史查询方法，验证前者逐条汇总成功/失败条数、后者按时间倒序返回

## 2. 后台页面

- [x] 2.1 新增 `nft/admin/claims.php` 页面骨架（权限校验 + 后台头尾布局），验证未登录管理员被拦截
- [x] 2.2 实现检索表单与结果表格（NFT编号/名称/城市/归属用户/区块ID/状态/认领时间 + 分页），验证筛选与翻页保留条件
- [x] 2.3 实现可疑认领筛查视图（阈值输入 + 结果列表 + 认领与注册时间差），验证默认阈值 300 秒且可调整
- [x] 2.4 实现单条订正交互（目标用户输入 + 原因必填 + 提交），验证目标用户不存在/未变化/原因空缺三种情况均被拒绝并提示
- [x] 2.5 实现批量订正交互（筛选结果整批 / 按来源用户+时间范围），验证执行前展示受影响条数并要求确认
- [x] 2.6 实现订正历史列表，验证展示操作人、原因、原归属与新归属

## 3. 集成

- [x] 3.1 在 `shared/admin/admin-menu-config.php` 新增「认领管理」菜单项，验证菜单可进入新页面
- [x] 3.2 页面在审计表缺失时捕获异常并提示需先执行建表语句，验证不出现致命错误

## 4. 验证

- [ ] 4.1 端到端验证：对 133 号执行单条订正 user 1 → user 390，前台 `nft/133.html` 认领人显示为 user 390
- [ ] 4.2 验证订正后生成审计记录，且订正历史中可见本次操作
- [ ] 4.3 验证正常认领（认领时间远晚于注册时间）不出现在可疑认领结果中
