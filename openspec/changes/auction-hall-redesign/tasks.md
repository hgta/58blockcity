## 1. 数据层与迁移

- [x] 1.1 编写 `init/migrate-auction-v2.sql`：`auctions` 增列 `bid_count`、`bidder_count`、`watch_count`（默认 0）、`final_price`、`sold_at`、`extend_count`（默认 0）、`auto_extend_seconds`、`extend_window_seconds`、`max_extend_times`、`max_extend_seconds`；`auction_bids` 增列 `prev_bidder_id`。迁移须可重复执行（判存在再 `ALTER`）；验证：在测试库连续执行两次均无报错
- [x] 1.2 在迁移中加入 `auction_watches(auction_id, user_id)` 建表与 `(auction_id, user_id)` 唯一键；验证：重复插入同一用户同一拍品被唯一键拒绝
- [x] 1.3 在迁移中回填存量数据：按 `auction_bids` 分组回写 `bid_count`、`bidder_count`，并为已结束拍品回填 `final_price`/`sold_at`；验证：抽样比对聚合结果与回填值一致
- [x] 1.4 提供计数重算 SQL（对账脚本），验证：手动制造一处计数偏差后可被重算脚本纠正

## 2. 后端机制（Auction.php）

- [x] 2.1 在 `placeBid()` 成功路径内维护 `bid_count`、`bidder_count`（首口出价才 `bidder_count+1`），并写入本次的 `prev_bidder_id`；验证：多名用户多次出价后，两个计数与去重统计一致
- [x] 2.2 在 `placeBid()` 内实现自动延时：剩余 ≤ `extend_window_seconds` 且未达 `max_extend_times`/`max_extend_seconds` 时顺延 `end_time` 并递增 `extend_count`；验证：尾段出价后 `end_time` 后移且 `extend_count` 增加，超限后不再顺延
- [x] 2.3 新增出价统计查询（一条 SQL 返回出价条数与去重竞拍人数），供首次渲染与重算兜底；验证：与 1.3 回填口径一致
- [x] 2.4 `getBids()` 排序改为时间倒序，并扫描全部调用方（`bid/view.php`、`bid/my.php`、管理端）确认无依赖金额序的展示；对确有需要处改为显式参数；验证：详情页叫价流最新出价在顶部，其他调用方展示无回归
- [x] 2.5 修复 `getActiveAuctions()` 的 N+1：改为 `IN (...)` 批量取 `blocks`/`nft_city_user` 关联信息并在内存映射；验证：开启查询日志，拍品数量增加时查询条数不随之线性增长
- [x] 2.6 接入「被超越」通知：领先者变更且原领先者非卖家、非新领先者本人时，向原领先者发送 `auction_outbid` 通知并附带详情页链接；验证：A 领先、B 加价后 A 收到通知；A 自行加价时无人收到通知
- [x] 2.7 在结算完成后补发「得标 / 流拍」通知，且不改动 `settle()` 结算结果；验证：得标者收到得标通知，流拍拍品相关方收到流拍通知

## 3. 关注与统计接口

- [x] 3.1 实现关注/取消关注接口（登录校验、事务内增删 `auction_watches` 并同步 `watch_count`，幂等不重复计数）；验证：连续关注同一拍品关注数只加一，取消后回退
- [x] 3.2 新增详情页轮询只读端点，返回 `server_now`、当前价、出价次数、竞拍人数、关注数、`end_at`、`extend_count`、本人状态与最近出价；验证：出价后一个周期内端点返回的数值随之更新
- [x] 3.3 查询列表接口按排序维度（热拍中/即将结束/价格/最新）返回结果；验证：切换每个维度后列表顺序符合预期

## 4. 视觉主题基建

- [x] 4.1 新增 `bid/assets/css/auction.css`，定义暗场 token（背景/卡面/分隔线/文字/琥珀-红-绿语义色）并限定在 `.auction-shell` 作用域内；验证：拍卖页内容区为暗场，共享导航与其他子站样式不受影响
- [x] 4.2 在 `bid/includes/header.php` 引入子站 CSS/JS，确认不覆盖主站样式；验证：打开首页与详情页样式加载正常、无控制台报错
- [x] 4.3 建立基础组件样式：拍品卡（含状态光环）、竞价面板、按钮、标签、数字等宽（`tabular-nums`）、悬停与追光效果；验证：同类组件在各页面视觉一致，倒计时变化不引起布局抖动
- [x] 4.4 补齐响应式规则（桌面多列 / 移动单列，关键信息首屏可见）；验证：在窄屏与桌面端分别浏览首页与详情页，无溢出、关键信息完整

## 5. 倒计时与实时刷新前端

- [x] 5.1 新增 `bid/assets/js/auction.js` 倒计时模块：读取 `end_at` 与页面级 `server_now` 计算偏移、逐秒自走、按剩余时间切三档配色与脉冲；验证：修改本机时间后剩余时间仍与服务端一致
- [x] 5.2 实现倒计时归零处理：停止计时并触发一次刷新，以服务端结果为准（顺延则显示新剩余时间，否则显示已落槌）；验证：模拟归零后界面正确切换
- [x] 5.3 实现详情页 3 秒轮询：仅增量更新当前价/计数/叫价流，`document.hidden` 时暂停、恢复可见立即拉取；验证：他人出价后界面自动更新，且用户输入中的出价金额与焦点不被打断

## 6. 首页竞价大厅

- [x] 6.1 重写 `bid/index.php` 首屏主推位（大图、编号、当前价与涨幅、倒计时、真实热度、直接出价入口），并处理无进行中拍品的空状态；验证：存在/不存在进行中拍品两种情况下页面均正确
- [x] 6.2 实现「即将结束」横向滑轨（按剩余时间升序）；验证：滑轨顺序与剩余时间一致且可横向滑动进入详情
- [x] 6.3 重写拍品卡（缩略图、标题、编号、当前价、倒计时、真实出价次数、竞拍人数、关注数、状态光环）；验证：出价次数与竞拍人数来自真实聚合，非 0/1
- [x] 6.4 接入排序/筛选（热拍中/即将结束/价格/最新）与「刚落槌」成交榜（含相对时间）；验证：切换维度列表重排，无成交记录时区域正常隐藏

## 7. 详情页竞价台

- [x] 7.1 重写 `bid/view.php` 竞价台布局：大图/画廊、编号标题、倒计时、当前价与涨幅、领先者标识、下一口价；验证：进行中与无人出价两种状态展示正确
- [x] 7.2 实现领先者状态与自身关联（「你正领先」高亮 / 「你已被超越」提示 + 再次出价入口）；验证：分别以领先者与他人身份登录查看呈现对应状态
- [x] 7.3 实现出价输入与快捷加价（+1 档 / +5 档 / 自定义），提交前校验不低于下一口价；验证：快捷按钮按加价幅度填充，低于门槛提交被拒并提示最低金额
- [x] 7.4 实现底价进度展示（底价未达 / 已过底价）并在跨越时更新；验证：设有底价的拍品在当前价跨越底价时状态切换，未设底价时不显示该模块
- [x] 7.5 实现实时叫价流（时间倒序、匿名标识、相对时间、新条目入场动效、本人高亮）；验证：新出价出现在顶部并带动效，本人出价被标记
- [x] 7.6 出价成功/失败的即时反馈（更新当前价与「你已领先」、失败给出具体原因）；验证：成功与各类失败场景均给出可理解反馈且状态不错乱
- [x] 7.7 补全详情页下方信息区（拍品故事/资产详情、卖家信息、拍卖规则含自动延时与出价门槛说明）；验证：规则文案与实际机制参数一致

## 8. 我的拍卖双视角

- [x] 8.1 重写 `bid/my.php` 为双视角（卖家看板 / 我的竞价）并支持切换；验证：两个视角仅展示对应数据
- [x] 8.2 卖家看板：状态、当前价、出价次数、竞拍人数、关注数、进行中倒计时/已结束成交结果，并标识新出价；验证：收到新出价的拍品被标记
- [x] 8.3 竞价视角按状态分组（我领先 / 已被超越 / 已得标 / 已结束未得标或流拍），被超越项提供再次出价入口；验证：各状态归组正确且入口可用
- [x] 8.4 两视角空状态与行动引导（去竞价大厅 / 发布拍品）；验证：无数据时展示对应引导

## 9. 发布页与联调验收

- [x] 9.1 适配 `bid/create.php` 暗场主题，并在发布表单中体现自动延时采用全局默认值（不开放卖家自定义）；验证：发布页样式统一且默认参数生效
- [ ] 9.2 全流程联调：发布 → 多人出价 → 尾段触发顺延 → 被超越通知 → 落槌 → 得标通知 → 各列表状态一致；验证：走通全链路且计数、状态、通知与展示一致
- [ ] 9.3 性能与压力核对：开启慢查询/查询计数，确认首页无 N+1、轮询端点无聚合放大；验证：拍品与出价量增长时响应时间保持可接受
- [ ] 9.4 兼容性核对：共享导航与其他子站样式无回归，`getBids()` 改序后既有调用方展示正常；验证：巡检主站导航、BCT 与拍卖站页面
