## 1. 批量订单解析器（可复用组件）

- [x] 1.1 新增批量订单解析器类（如 `classes/BatchOrderParser.php`），实现文本归一与切分：全角空格替换为半角、按 `\r\n|\r|\n` 切行、跳过空行与 `#` / `//` 注释行、按 `\s+` 分列，验证用「吐鲁番 65000 0.03」多行样例可解析出正确行数且注释行被忽略
- [x] 1.2 实现三列约束与字段校验：列数不为 3 归类为「应为『城市 数量 价格』三列」，数量非正整数或超出 `1..1000000` 归类为「数量无效」，价格非正数归类为「价格无效」，验证用故意构造的 4 类错误行分别落入正确分类
- [x] 1.3 实现城市匹配：先按 `cities.name` 精确匹配，失败再按 `cities.pinyin` 匹配（`LIMIT 1`），均未命中归类为「城市不存在」；验证「吐鲁番」与「jingtan」均可命中、「不存在的城市名」落入 missing
- [x] 1.4 实现按「城市 + 价格」合并与数量累加，且合并先于校验；验证 `吐鲁番 30000 0.03` + `吐鲁番 20000 0.03` 归并为一条数量 50000 的挂单，而 `吐鲁番 20000 0.04` 保留为独立第二条
- [x] 1.5 改造 `bct/admin/city_prices.php` 的批量分支改为调用新解析器，并验证其原有 `updated/skipped/missing/invalid` 四分类输出与改造前完全一致（回归检查）

## 2. 批量预校验（只读接口）

- [x] 2.1 新增批量预校验接口（如 `bct/api/parse_batch_orders.php`），接收文本、方向、交易方式与联系方式，返回逐条挂单的城市匹配状态、数量/价格合法性；验证响应为 JSON 且包含逐条状态
- [x] 2.2 确认预校验不做余额校验（发布订单无需验证余额）：验证余额为 0 的城市卖单仍标记为可发布、不出现「可用余额不足」
- [x] 2.3 确认预校验全程为只读：验证调用接口前后 `bct_orders` 行数与 `user_bct_account` 余额均无变化

## 3. 批量提交与部分成功

- [x] 3.1 新增批量提交处理（如 `bct/process_batch_orders.php`），执行 CSRF 校验与方向/交易方式白名单校验，拒绝 `platform` 与买卖混合输入；验证非法提交被拒绝且无订单写入
- [x] 3.2 实现逐条 `createOrder()` 编排：提交前对每条复检（城市、数量、价格），复检通过则创建订单并记录订单号，失败则记录原因后继续下一条；验证部分行失败时成功条数与失败条数之和等于总挂单数
- [x] 3.3 实现 `mediator` 整体中介与 `direct` 联系方式传递：验证中介交易时所有订单共用同一 `mediator_id`，直接交易时缺失联系方式会被拒绝
- [x] 3.4 实现提交结果汇总并在页面展示：每条挂单可见最终状态（成功含订单标识 / 失败含原因），并汇总成功与失败条数；验证结果区逐条可读且计数正确

## 4. 批量模式 UI（`bct/trade.php`）

- [x] 4.1 在 `bct/trade.php` 增加「单条模式 / 批量模式」切换，批量模式展示方向选择（全部买入 / 全部卖出）、交易方式（直接交易 / 中介交易，不含平台交易）、联系方式与多行文本输入区；验证切换后单条模式行为与改造前一致
- [x] 4.2 实现「识别」交互：提交文本到预校验接口并将返回结果渲染为可编辑、可删行的预览表，逐行标出城市与格式状态；验证出现「城市不存在」「格式错误」时预览表对应行有明确标识
- [x] 4.3 实现「确认发布」交互：将预览表内容提交到批量提交处理并展示逐条结果；验证 9 行样例在部分城市无效时仍能发布有效行并展示汇总

## 5. 交易数量上限调整

- [x] 5.1 将 `bct/trade.php` 单条模式数量输入的 `max` 与提示文案由 100,000 改为 1,000,000，验证页面文案与控件上限一致
- [x] 5.2 在 `bct/process_order.php` 补上服务端数量上限校验（`1..1000000`）；验证直接构造 `amount=2000000` 的请求被拒绝并返回数量超出范围的提示
- [x] 5.3 统一单条与批量的上限常量为同一取值来源，验证两处校验不会出现不一致的边界

## 5.4 移除余额校验（决策修正）

- [x] 5.4.1 移除 `BCTOrder::createOrder()` 中的卖单余额校验与 `throw`，验证卖单在余额为 0 时仍能创建订单
- [x] 5.4.2 移除 `bct/process_order.php`（单条）与批量预校验/提交中的余额校验，验证单条与批量口径一致

## 6. 端到端验证

- [ ] 6.1 用用户提供的 9 行样例（含自建城市「鲸探」）走完整流程：粘贴 → 识别 → 确认发布，验证自建城市可正常匹配并生成订单，且逐条结果与预览状态一致
- [x] 6.2 验证边界与容错：全角空格、制表符、`#` 注释、四列错误行、同名同价累加、同名不同价独立、超上限数量各场景行为符合 `specs/bct/` 下两份规格的场景描述
- [ ] 6.3 回归验证单条发布流程（`?city=` 选城市 → 提交）行为未受影响，且数量上限校验正常生效、卖单发布不再校验余额
